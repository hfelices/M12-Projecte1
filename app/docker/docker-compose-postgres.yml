version: '3.8'
services:
  postgres_server:
    image: postgres:16.0
    environment:
      POSTGRES_DB: 2dd10_pg
      POSTGRES_USER: 2dd10
      POSTGRES_PASSWORD: kwcBOK1f5OGhrD+e
    ports:
      - 5432:5432
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - adminer-network

  adminer:
    image: adminer
    ports:
      - 8080:8080
    environment:
      ADMINER_DEFAULT_SERVER: postgres_server
    depends_on:
      - postgres_server
    networks:
      - adminer-network

volumes:
  postgres-data:

networks:
  adminer-network: